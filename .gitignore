node_modules/
.DS_Store
*.log

/artifacts/*
/docs
/.playwright-mcp
/admin/.draft

# NOTE: dist/ is intentionally NOT ignored — it is committed so that
# pnpm git-dependency consumers receive the emitted tokens without a
# build step. Regenerate with `pnpm build` after editing tokens/.

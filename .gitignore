node_modules/
.DS_Store
*.log

/artifacts/*
/docs
/.playwright-mcp
/dist/
# build/current/ is a local validation snapshot regenerated by `pnpm build`
# (not consumed by check-artifact or shipped — release/ is the committed contract).
/build/current/
scripts/claude-deepseek-session.sh
